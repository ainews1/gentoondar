---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - lib/domain/repositories/task_repository.dart
  - lib/domain/usecases/create_task.dart
  - lib/domain/usecases/update_task.dart
  - lib/domain/usecases/delete_task.dart
  - lib/domain/usecases/get_task_by_id.dart
  - lib/domain/usecases/get_tasks_by_date.dart
  - lib/data/repositories/task_repository_impl.dart
  - lib/presentation/providers/task_providers.dart
  - lib/core/error/failures.dart
  - lib/core/usecase/usecase.dart
  - pubspec.yaml
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]

must_haves:
  truths:
    - "Business logic is isolated from UI and data layers through use cases"
    - "Repository abstracts data source details from business logic"
    - "Riverpod providers manage task state reactively"
    - "Error handling propagates properly through all layers"
  artifacts:
    - path: "lib/domain/repositories/task_repository.dart"
      provides: "Repository interface for task operations"
      exports: ["TaskRepository"]
    - path: "lib/data/repositories/task_repository_impl.dart"
      provides: "Concrete repository implementation"
      exports: ["TaskRepositoryImpl"]
    - path: "lib/domain/usecases/"
      provides: "Business logic operations for task management"
      exports: ["CreateTask", "UpdateTask", "DeleteTask", "GetTaskById", "GetTasksByDate"]
    - path: "lib/presentation/providers/task_providers.dart"
      provides: "Riverpod providers for task state management"
      exports: ["taskRepositoryProvider", "taskListProvider", "createTaskProvider"]
  key_links:
    - from: "lib/presentation/providers/task_providers.dart"
      to: "lib/domain/usecases/"
      via: "provider calls to use cases"
      pattern: "ref\\.read\\(.*UseCase\\)"
    - from: "lib/data/repositories/task_repository_impl.dart"
      to: "lib/data/datasources/local/task_local_datasource.dart"
      via: "data source injection"
      pattern: "TaskLocalDataSource.*datasource"
---

<objective>
Build the repository pattern and use case layer with Riverpod state management for reactive task operations

Purpose: Create the business logic layer that encapsulates task operations and provides clean separation between UI and data
Output: Working repository, use cases, and Riverpod providers for task CRUD operations
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<interfaces>
<!-- Key types from Plan 01 that this plan will use -->

From lib/domain/entities/task.dart:
```dart
class Task extends Equatable {
  final int id;
  final String title;
  final String? description;
  final DateTime startTime;
  final int durationMinutes;
  final bool isCompleted;
  final DateTime createdAt;
  final DateTime updatedAt;
}
```

From lib/data/models/task_model.dart:
```dart
class TaskModel {
  Task toEntity();
  factory TaskModel.fromEntity(Task task);
  Map<String, dynamic> toJson();
  factory TaskModel.fromJson(Map<String, dynamic> json);
}
```

From lib/data/datasources/local/task_local_datasource.dart:
```dart
class TaskLocalDataSource {
  Future<int> insertTask(TaskModel task);
  Future<void> updateTask(TaskModel task);
  Future<void> deleteTask(int id);
  Future<TaskModel?> getTaskById(int id);
  Future<List<TaskModel>> getTasksForDate(DateTime date);
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core abstractions and error handling</name>
  <files>lib/core/error/failures.dart, lib/core/usecase/usecase.dart, lib/domain/repositories/task_repository.dart, pubspec.yaml</files>
  <behavior>
    - Failure classes provide structured error handling across all layers
    - UseCase base class defines standard operation interface
    - TaskRepository interface abstracts all task data operations
    - Either type enables functional error handling without exceptions
  </behavior>
  <action>
    Add dependencies to pubspec.yaml:
    ```yaml
    dependencies:
      dartz: ^0.10.1
      riverpod: ^2.4.9
      flutter_riverpod: ^2.4.9
    ```

    Create lib/core/error/failures.dart:
    - Abstract Failure base class with message and code
    - DatabaseFailure, ValidationFailure, NotFoundFailure subclasses
    - Equatable mixin for failure comparison in tests

    Create lib/core/usecase/usecase.dart:
    - Abstract UseCase<Type, Params> base class
    - call() method returning Future<Either<Failure, Type>>
    - NoParams class for use cases without parameters

    Create lib/domain/repositories/task_repository.dart interface:
    - createTask(Task task) -> Future<Either<Failure, Task>>
    - updateTask(Task task) -> Future<Either<Failure, Task>>
    - deleteTask(int id) -> Future<Either<Failure, void>>
    - getTaskById(int id) -> Future<Either<Failure, Task>>
    - getTasksByDate(DateTime date) -> Future<Either<Failure, List<Task>>>
    - getAllTasks() -> Future<Either<Failure, List<Task>>>
  </action>
  <verify>
    <automated>dart test test/unit/core/ test/unit/domain/repositories/ --reporter=compact</automated>
  </verify>
  <done>Core abstractions with error handling, UseCase base class, and TaskRepository interface created with tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repository and use cases</name>
  <files>lib/data/repositories/task_repository_impl.dart, lib/domain/usecases/create_task.dart, lib/domain/usecases/update_task.dart, lib/domain/usecases/delete_task.dart, lib/domain/usecases/get_task_by_id.dart, lib/domain/usecases/get_tasks_by_date.dart</files>
  <behavior>
    - Repository implementation converts between TaskModel and Task entity
    - Use cases encapsulate single business operations with validation
    - Error handling converts database exceptions to domain failures
    - Repository properly handles data source errors and null returns
  </behavior>
  <action>
    Create lib/data/repositories/task_repository_impl.dart:
    - Implements TaskRepository interface
    - Injects TaskLocalDataSource dependency
    - Converts TaskModel to/from Task entities using existing methods
    - Wraps database operations in try-catch, converting exceptions to appropriate Failures
    - Handle null returns from data source (e.g., task not found)

    Create use cases in lib/domain/usecases/:
    - CreateTask: validates task data (title not empty, duration > 0), calls repository.createTask
    - UpdateTask: validates task exists and data is valid, calls repository.updateTask  
    - DeleteTask: takes task ID, calls repository.deleteTask
    - GetTaskById: takes ID, returns Either<Failure, Task>
    - GetTasksByDate: takes DateTime, returns Either<Failure, List<Task>>

    Each use case extends UseCase base class and implements call() method with proper parameter classes.
  </action>
  <verify>
    <automated>dart test test/unit/data/repositories/ test/unit/domain/usecases/ --reporter=compact</automated>
  </verify>
  <done>TaskRepositoryImpl with error handling and all use cases implemented with comprehensive unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Setup Riverpod providers for state management</name>
  <files>lib/presentation/providers/task_providers.dart</files>
  <action>
    Create lib/presentation/providers/task_providers.dart with Riverpod providers:
    
    Data layer providers:
    - taskLocalDataSourceProvider: Provides TaskLocalDataSource instance
    - taskRepositoryProvider: Provides TaskRepositoryImpl with injected data source

    Use case providers:
    - createTaskProvider: AsyncNotifierProvider for CreateTask use case
    - updateTaskProvider: AsyncNotifierProvider for UpdateTask use case
    - deleteTaskProvider: AsyncNotifierProvider for DeleteTask use case
    - getTaskByIdProvider: FutureProviderFamily<Task?, int> for single task lookup
    - getTasksByDateProvider: FutureProviderFamily<List<Task>, DateTime> for date-based task lists

    State management providers:
    - taskListProvider: AsyncNotifierProvider managing task list state with methods:
      - loadTasks(DateTime date)
      - addTask(Task task)
      - updateTask(Task task)
      - removeTask(int id)
    - selectedDateProvider: StateProvider<DateTime> for calendar integration

    Include proper error handling in providers, converting Either<Failure, T> to AsyncValue<T>.
    Use ref.invalidate() to refresh dependent providers after mutations.
  </action>
  <verify>
    <automated>dart test test/unit/presentation/providers/ --reporter=compact && flutter analyze</automated>
  </verify>
  <done>Riverpod providers configured for reactive task state management with proper dependency injection</done>
</task>

</tasks>

<verification>
Verify repository pattern and state management:
- `dart test test/unit/data/repositories/ --coverage` - Repository tests pass
- `dart test test/unit/domain/usecases/ --coverage` - Use case tests pass
- `dart test test/unit/presentation/providers/` - Provider tests pass
- `flutter analyze` - No static analysis warnings
- Check dependency injection: Repository receives data source, use cases receive repository
- Verify error handling: Database errors convert to domain failures
</verification>

<success_criteria>
- Repository pattern properly abstracts data layer from business logic
- Use cases encapsulate business operations with validation and error handling
- Riverpod providers manage task state reactively with proper dependency injection
- Error handling flows correctly through all layers (database -> repository -> use case -> provider)
- Provider state updates trigger UI rebuilds when tasks are modified
- All unit tests pass with good coverage across repository and use case layers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>