---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - lib/presentation/screens/task_list_screen.dart
  - lib/presentation/widgets/tasks/task_card.dart
  - lib/presentation/widgets/tasks/task_form.dart
  - lib/presentation/widgets/tasks/task_list.dart
  - lib/presentation/navigation/app_router.dart
  - lib/main.dart
  - lib/presentation/theme/app_theme.dart
autonomous: false
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, UI-06]

must_haves:
  truths:
    - "User can create tasks through form with title, description, date, time, duration"
    - "User can edit existing tasks and changes persist immediately"
    - "User can delete tasks with confirmation and data is permanently removed"
    - "User can mark tasks complete/incomplete with visual status change"
    - "Keyboard input works smoothly without breaking form layouts"
    - "Task list displays properly with real data from SQLite database"
  artifacts:
    - path: "lib/presentation/screens/task_list_screen.dart"
      provides: "Main screen displaying task list with navigation"
      exports: ["TaskListScreen"]
    - path: "lib/presentation/widgets/tasks/task_form.dart"
      provides: "Form for creating and editing tasks"
      exports: ["TaskForm"]
    - path: "lib/presentation/widgets/tasks/task_card.dart"
      provides: "Individual task display with actions"
      exports: ["TaskCard"]
    - path: "lib/presentation/widgets/tasks/task_list.dart"
      provides: "Scrollable list of task cards"
      exports: ["TaskList"]
  key_links:
    - from: "lib/presentation/widgets/tasks/task_form.dart"
      to: "lib/presentation/providers/task_providers.dart"
      via: "form submission to create/update providers"
      pattern: "ref\\.read\\(.*taskProvider\\)"
    - from: "lib/presentation/widgets/tasks/task_card.dart"
      to: "lib/presentation/providers/task_providers.dart"
      via: "task completion and deletion actions"
      pattern: "ref\\.read\\(.*Provider\\)"
    - from: "lib/presentation/widgets/tasks/task_list.dart"
      to: "lib/presentation/providers/task_providers.dart"
      via: "task list state watching"
      pattern: "ref\\.watch\\(taskListProvider\\)"
---

<objective>
Create the task management UI with forms, lists, and navigation that provides full CRUD functionality over the established data layer

Purpose: Deliver working task management interface that validates the complete data flow and provides essential user functionality
Output: Complete task management app with create, read, update, delete operations and proper keyboard/touch handling
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/01-02-PLAN.md
</context>

<interfaces>
<!-- Key types from previous plans that this plan will use -->

From lib/domain/entities/task.dart:
```dart
class Task {
  final int id;
  final String title;
  final String? description; 
  final DateTime startTime;
  final int durationMinutes;
  final bool isCompleted;
  
  Task copyWith({...});
}
```

From lib/presentation/providers/task_providers.dart:
```dart
// State management providers
final taskListProvider = AsyncNotifierProvider<TaskListNotifier, List<Task>>();
final selectedDateProvider = StateProvider<DateTime>((ref) => DateTime.now());
final createTaskProvider = AsyncNotifierProvider<CreateTaskNotifier, void>();
final updateTaskProvider = AsyncNotifierProvider<UpdateTaskNotifier, void>();
final deleteTaskProvider = AsyncNotifierProvider<DeleteTaskNotifier, void>();
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Setup navigation and app structure</name>
  <files>lib/main.dart, lib/presentation/navigation/app_router.dart, lib/presentation/theme/app_theme.dart, pubspec.yaml</files>
  <action>
    Add dependencies to pubspec.yaml:
    ```yaml
    dependencies:
      go_router: ^13.0.0
      flutter_datetime_picker_plus: ^2.1.0
      
    dev_dependencies:
      flutter_test:
    ```

    Create lib/presentation/theme/app_theme.dart:
    - Material 3 theme with primary color scheme
    - Typography scale with proper text styles for forms and cards
    - Button styles for form actions and task cards
    - Input decoration theme for text fields with proper focus handling
    - Card theme for task display with elevation and border radius

    Create lib/presentation/navigation/app_router.dart:
    - GoRouter configuration with routes:
      - '/' -> TaskListScreen (home)
      - '/task/new' -> TaskForm (create mode)
      - '/task/:id/edit' -> TaskForm (edit mode)
    - Error handling for invalid routes
    - Route transitions and navigation guards

    Update lib/main.dart:
    - ProviderScope wrapper for Riverpod
    - MaterialApp.router with GoRouter
    - App theme configuration
    - Database initialization before app start
  </action>
  <verify>
    <automated>flutter analyze && flutter test test/widget/navigation_test.dart</automated>
  </verify>
  <done>App structure with navigation, theming, and Riverpod integration configured and tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build task form with validation and keyboard handling</name>
  <files>lib/presentation/widgets/tasks/task_form.dart</files>
  <behavior>
    - Form validates title (required, 1-100 chars), duration (1-480 minutes)
    - Date/time pickers work with touch and keyboard navigation  
    - Form handles both create and edit modes based on optional Task parameter
    - Keyboard dismissal works properly and doesn't break layout
    - Form submission calls appropriate providers and navigates on success
  </behavior>
  <action>
    Create lib/presentation/widgets/tasks/task_form.dart:
    - StatefulWidget with form key and controllers for title, description
    - Constructor: TaskForm({Task? task}) for create/edit modes
    - Form fields:
      - Title: TextFormField with validation (required, max 100 chars)
      - Description: TextFormField multiline, optional, max 500 chars
      - Date: GestureDetector showing DatePicker, displays selected date
      - Time: GestureDetector showing TimePicker, displays selected time
      - Duration: DropdownButtonFormField with preset options (15, 30, 45, 60, 90, 120 minutes)
    - Save button calls createTaskProvider or updateTaskProvider
    - Cancel button navigates back without changes
    - Loading state during submission with disabled form
    - Error handling displays SnackBar for failures
    - Keyboard handling: TextInputAction.next, FocusScope navigation, dismiss on tap outside
    - Form auto-population in edit mode from task parameter
  </action>
  <verify>
    <automated>flutter test test/widget/task_form_test.dart --reporter=compact</automated>
  </verify>
  <done>TaskForm with validation, keyboard handling, and create/edit modes working with comprehensive widget tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build task display components and list</name>
  <files>lib/presentation/widgets/tasks/task_card.dart, lib/presentation/widgets/tasks/task_list.dart, lib/presentation/screens/task_list_screen.dart</files>
  <behavior>
    - Task card displays all task fields with proper formatting and completion status
    - Task completion toggle updates state immediately with visual feedback
    - Task deletion shows confirmation dialog and removes from list
    - Task list efficiently renders large numbers of tasks with ListView.builder
    - Empty state shows helpful message when no tasks exist
    - Pull-to-refresh reloads tasks from database
  </behavior>
  <action>
    Create lib/presentation/widgets/tasks/task_card.dart:
    - Card widget displaying task with Material 3 styling
    - Title and description with proper text styles and overflow handling
    - Date and time formatted as "MMM dd, yyyy at HH:mm"
    - Duration displayed as "X minutes" or "X hours Y minutes"
    - Completion checkbox with immediate state update via updateTaskProvider
    - Edit button navigating to TaskForm in edit mode
    - Delete button with showDialog confirmation, calls deleteTaskProvider
    - Visual completion state: strikethrough title, muted colors when completed
    - Proper accessibility semantics and touch targets (min 44pt)

    Create lib/presentation/widgets/tasks/task_list.dart:
    - Consumer widget watching taskListProvider
    - Handle AsyncValue states: loading (CircularProgressIndicator), error (retry button), data
    - ListView.builder for efficient rendering of task cards
    - Empty state widget with friendly message and "Add Task" button
    - RefreshIndicator wrapping ListView for pull-to-refresh
    - Proper error handling with user-friendly error messages

    Create lib/presentation/screens/task_list_screen.dart:
    - Scaffold with AppBar containing title and "Add Task" action button
    - Body containing TaskList widget
    - FloatingActionButton for quick task creation
    - Date selector showing current selected date (preparation for calendar integration)
    - Provider initialization and error boundary handling
  </action>
  <verify>
    <automated>flutter test test/widget/task_card_test.dart test/widget/task_list_test.dart test/widget/task_list_screen_test.dart --reporter=compact</automated>
  </verify>
  <done>Task display components with proper Material Design, state management integration, and comprehensive widget tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete task management application with CRUD operations, forms, and database persistence</what-built>
  <how-to-verify>
    1. Run the application: `flutter run --debug`
    2. Test task creation:
       - Tap "Add Task" button or FAB
       - Fill in title "Test Task", description "Test Description"
       - Select tomorrow's date and a time
       - Choose 30 minutes duration
       - Tap Save - should navigate back to list
    3. Test task display:
       - Verify new task appears in list with correct information
       - Check date/time formatting and duration display
    4. Test task completion:
       - Tap checkbox to mark complete - title should strikethrough
       - Tap again to mark incomplete - styling should revert
    5. Test task editing:
       - Tap Edit button on a task
       - Modify title and duration
       - Save changes - should update in list
    6. Test task deletion:
       - Tap Delete button on a task
       - Confirm in dialog - task should disappear from list
    7. Test persistence:
       - Hot restart app - tasks should still be there
       - Force quit and reopen app - data should persist
    8. Test keyboard handling:
       - Focus text fields - keyboard should appear smoothly
       - Tap outside form - keyboard should dismiss
       - Use Tab/Next to navigate between fields
    9. Test empty state:
       - Delete all tasks - should show empty state message
  </how-to-verify>
  <resume-signal>Type "approved" if all functionality works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
End-to-end verification of Phase 1 objectives:
- `flutter test test/widget/ --coverage` - All widget tests pass
- `flutter test test/integration/` - Integration tests pass  
- `flutter analyze` - No static analysis warnings
- `flutter run --debug` - App starts successfully
- Manual testing of complete CRUD workflow
- Database persistence verification across app restarts
- Keyboard interaction and accessibility testing
</verification>

<success_criteria>
- User can create tasks with all required fields through intuitive form interface
- Task list displays all tasks with proper formatting and Material Design styling
- Task completion toggles work immediately with visual feedback
- Task editing loads existing data and saves changes correctly
- Task deletion requires confirmation and removes data permanently
- App starts quickly (<2 seconds) and loads existing tasks from SQLite
- Keyboard input works smoothly without layout issues or focus problems
- Database persistence survives app restarts and device reboots
- All UI components follow Material Design guidelines and accessibility standards
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>