---
phase: 04-search-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "lib/domain/usecases/search_tasks.dart",
  "lib/presentation/screens/search_screen.dart", 
  "lib/presentation/providers/search_providers.dart",
  "lib/presentation/widgets/search/highlighted_task_card.dart",
  "lib/presentation/navigation/app_router.dart",
  "lib/presentation/screens/task_list_screen.dart"
]
autonomous: true
requirements: [SEARCH-01, SEARCH-02]
user_setup: []

must_haves:
  truths:
    - "User can search tasks by title text with real-time results appearing as they type"
    - "User can search tasks by description text with real-time results"
    - "Search results highlight matching text within task titles and descriptions"
    - "Search screen auto-focuses keyboard when opened for immediate typing"
    - "Search works across all tasks regardless of date"
  artifacts:
    - path: "lib/domain/usecases/search_tasks.dart"
      provides: "Search use case following clean architecture pattern"
      min_lines: 40
    - path: "lib/presentation/screens/search_screen.dart"  
      provides: "Dedicated search screen with Material 3 SearchBar"
      min_lines: 120
    - path: "lib/presentation/providers/search_providers.dart"
      provides: "Riverpod search state management"
      min_lines: 80
    - path: "lib/presentation/widgets/search/highlighted_task_card.dart"
      provides: "TaskCard variant with text highlighting"
      min_lines: 60
  key_links:
    - from: "lib/presentation/screens/search_screen.dart"
      to: "lib/presentation/providers/search_providers.dart"
      via: "Consumer widget watching search state"
      pattern: "ref.watch\\(searchStateProvider\\)"
    - from: "lib/presentation/providers/search_providers.dart"
      to: "lib/domain/usecases/search_tasks.dart"
      via: "provider calling use case"
      pattern: "ref.read\\(searchTasksUseCaseProvider\\)"
    - from: "lib/presentation/screens/search_screen.dart"
      to: "/search route"
      via: "navigation registration"
      pattern: "GoRoute.*path.*\\/search"
---

<objective>
Implement comprehensive text search functionality with real-time results and enhanced text highlighting.

Purpose: Enable users to quickly find tasks by title or description text, fulfilling the core search requirements with immediate visual feedback.
Output: Working search screen with highlighted results accessible via app bar search icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-polish/04-CONTEXT.md
@.planning/phases/04-search-polish/04-RESEARCH.md

# Existing patterns to follow
@lib/presentation/providers/task_providers.dart
@lib/presentation/screens/task_list_screen.dart
@lib/presentation/widgets/tasks/task_card.dart
@lib/domain/usecases/get_tasks_by_date.dart
@lib/presentation/navigation/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create search use case following clean architecture</name>
  <files>lib/domain/usecases/search_tasks.dart</files>
  <action>
    Create SearchTasks use case following the existing pattern from get_tasks_by_date.dart. The use case should:
    - Accept SearchTasksParams with searchTerm property
    - Return Either<Failure, List<Task>>
    - Call repository.searchTasks() method (already exists in TaskLocalDataSource)
    - Follow the exact same structure as GetTasksByDate with proper error handling
    - Include proper documentation explaining the case-insensitive search behavior
    
    Use the existing TaskRepository interface - the searchTasks method will need to be added to the interface and implementation in the next task.
  </action>
  <verify>
    <automated>grep -q "class SearchTasks" lib/domain/usecases/search_tasks.dart && grep -q "Either<Failure, List<Task>>" lib/domain/usecases/search_tasks.dart</automated>
  </verify>
  <done>SearchTasks use case exists with proper Either return type and follows clean architecture pattern</done>
</task>

<task type="auto">
  <name>Task 2: Implement search screen with Material 3 SearchBar and auto-focus</name>
  <files>lib/presentation/screens/search_screen.dart</files>
  <action>
    Create dedicated SearchScreen following Material 3 design and user decisions D-01, D-02, D-04, D-05 from CONTEXT.md:
    
    - Use Scaffold with AppBar titled "Search Tasks"
    - Implement Material 3 SearchBar widget with autoFocus: true (D-02)
    - Show "Enter search term" message when field is empty (D-05)  
    - Implement hybrid search: real-time for 3+ characters, submit button for shorter (D-04)
    - Use debouncing (300ms) to prevent excessive queries during typing
    - Display search results using existing TaskCard pattern but with highlighting
    - Handle loading states and empty results gracefully
    - Follow existing screen patterns from task_list_screen.dart structure
    
    Key implementation details:
    - Use Timer for debouncing search input
    - Consumer widget to watch searchStateProvider  
    - ListView.builder for results with proper key management
    - Handle keyboard focus and dismissal properly
  </action>
  <verify>
    <automated>grep -q "SearchBar" lib/presentation/screens/search_screen.dart && grep -q "autoFocus: true" lib/presentation/screens/search_screen.dart && grep -q "Consumer" lib/presentation/screens/search_screen.dart</automated>
  </verify>
  <done>SearchScreen exists with Material 3 SearchBar, auto-focus enabled, and proper state management integration</done>
</task>

<task type="auto">
  <name>Task 3: Create search state management with Riverpod providers</name>
  <files>lib/presentation/providers/search_providers.dart</files>
  <action>
    Create comprehensive search state management following the established Riverpod patterns from task_providers.dart:
    
    Provider structure:
    - searchTasksUseCaseProvider: Provides SearchTasks use case
    - searchQueryProvider: StateProvider for current search query string
    - searchResultsProvider: FutureProvider.family that triggers search when query changes
    - searchStateNotifierProvider: StateNotifier managing loading states, results, and query
    
    State management features:
    - Debounced search execution (only trigger for queries 3+ characters per D-04)
    - Loading state management during async search operations
    - Error handling following Either<Failure, List<Task>> pattern
    - Empty state handling per D-05 requirements
    - Results ordered by most recent first per D-07
    
    Follow the exact provider patterns, naming conventions, and error handling from existing task_providers.dart file.
  </action>
  <verify>
    <automated>grep -q "searchTasksUseCaseProvider" lib/presentation/providers/search_providers.dart && grep -q "StateNotifier" lib/presentation/providers/search_providers.dart && grep -q "FutureProvider" lib/presentation/providers/search_providers.dart</automated>
  </verify>
  <done>Search providers exist with proper state management, debouncing, and error handling following project patterns</done>
</task>

<task type="auto">
  <name>Task 4: Create highlighted task card component for search results</name>
  <files>lib/presentation/widgets/search/highlighted_task_card.dart</files>
  <action>
    Create HighlightedTaskCard widget that extends the existing TaskCard pattern with text highlighting per D-08, D-09:
    
    Features:
    - Accept Task object and searchTerm string as parameters
    - Use existing TaskCard as base structure for consistency  
    - Implement RichText highlighting using TextSpan as per RESEARCH.md examples
    - Highlight matching text in both title and description fields
    - Use yellow background (Colors.yellow.shade200) for highlighted terms
    - Case-insensitive highlighting using RegExp matching
    - Show snippet previews when description contains matches (D-09)
    - Maintain existing card styling, spacing, and accessibility properties
    
    Text highlighting implementation:
    - Create helper method _buildHighlightedText(String text, String searchTerm) 
    - Handle null/empty search terms gracefully (no highlighting)
    - Preserve original text style for non-highlighted portions
    - Ensure highlighting works with existing theme typography
  </action>
  <verify>
    <automated>grep -q "RichText" lib/presentation/widgets/search/highlighted_task_card.dart && grep -q "TextSpan" lib/presentation/widgets/search/highlighted_task_card.dart && grep -q "RegExp" lib/presentation/widgets/search/highlighted_task_card.dart</automated>
  </verify>
  <done>HighlightedTaskCard exists with proper text highlighting using RichText and maintains existing card design patterns</done>
</task>

<task type="auto">
  <name>Task 5: Add search route and navigation integration</name>
  <files>lib/presentation/navigation/app_router.dart, lib/presentation/screens/task_list_screen.dart</files>
  <action>
    Integrate search screen into navigation following user decision D-01 (app bar search icon access):
    
    In app_router.dart:
    - Add static const String search = '/search';
    - Add GoRoute for search screen path
    - Add static method AppNavigation.goToSearch(BuildContext context)
    - Follow existing route patterns and naming conventions
    
    In task_list_screen.dart:
    - Add search icon to AppBar actions
    - Connect search icon onPressed to navigation.goToSearch()
    - Use standard Material Icons.search icon
    - Add proper tooltip: 'Search tasks'
    - Maintain existing AppBar structure and styling
    
    Ensure search screen integrates cleanly with existing go_router navigation without breaking current flows.
  </action>
  <verify>
    <automated>grep -q "/search" lib/presentation/navigation/app_router.dart && grep -q "Icons.search" lib/presentation/screens/task_list_screen.dart && grep -q "SearchScreen" lib/presentation/navigation/app_router.dart</automated>
  </verify>
  <done>Search route registered in go_router and search icon added to task list app bar with proper navigation</done>
</task>

</tasks>

<verification>
- Search functionality accessible via app bar search icon in task list
- SearchBar auto-focuses keyboard when search screen opens
- Real-time search works for 3+ character queries with debouncing
- Search results highlight matching text in titles and descriptions
- Search covers all tasks regardless of date
- Results ordered by most recent first (creation date)
- Empty search shows "Enter search term" message
- All components follow existing Material 3 and Riverpod patterns
</verification>

<success_criteria>
- User can tap search icon in any screen and immediately start typing
- Search results appear within 500ms of typing and highlight matching text
- Search works across task titles and descriptions simultaneously
- UI maintains consistency with existing task display patterns
- No performance degradation during real-time search operations
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-polish/04-01-SUMMARY.md`
</output>