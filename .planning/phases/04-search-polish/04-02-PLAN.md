---
phase: 04-search-polish
plan: 02
type: execute  
wave: 1
depends_on: []
files_modified: [
  "lib/domain/usecases/filter_tasks_by_date_range.dart",
  "lib/domain/usecases/get_tasks_by_completion_status.dart", 
  "lib/presentation/widgets/search/search_filters.dart",
  "lib/presentation/screens/search_screen.dart",
  "lib/presentation/providers/search_providers.dart",
  "lib/presentation/screens/task_list_screen.dart",
  "lib/presentation/screens/main_screen.dart"
]
autonomous: true
requirements: [FILTER-01, FILTER-02, UI-04]
user_setup: []

must_haves:
  truths:
    - "User can filter tasks by date range using date picker controls"
    - "User can filter tasks by completion status with clear filter state indication"
    - "Screen reader users can navigate all calendar and task creation interfaces"
    - "All filtering controls follow WCAG 2.1 AA accessibility guidelines" 
    - "Search and filter state work together seamlessly"
  artifacts:
    - path: "lib/domain/usecases/filter_tasks_by_date_range.dart"
      provides: "Date range filtering use case"
      min_lines: 35
    - path: "lib/domain/usecases/get_tasks_by_completion_status.dart"
      provides: "Status filtering use case"  
      min_lines: 35
    - path: "lib/presentation/widgets/search/search_filters.dart"
      provides: "Filter controls UI with accessibility"
      min_lines: 100
    - path: "accessibility_audit_results.md"
      provides: "Comprehensive accessibility testing report"
      min_lines: 50
  key_links:
    - from: "lib/presentation/widgets/search/search_filters.dart"
      to: "showDatePicker()"
      via: "date range selection"
      pattern: "showDatePicker\\("
    - from: "lib/presentation/screens/search_screen.dart" 
      to: "lib/presentation/widgets/search/search_filters.dart"
      via: "filter widget integration"
      pattern: "SearchFilters\\("
    - from: "screen reader navigation"
      to: "Semantics widgets"
      via: "accessibility compliance"
      pattern: "Semantics\\("
---

<objective>
Implement date range and status filtering with comprehensive accessibility compliance for screen readers.

Purpose: Complete the search experience with filtering capabilities and ensure full WCAG 2.1 AA compliance for all users including those using assistive technologies.
Output: Fully accessible task management app with advanced filtering capabilities.
</objective>

<execution_context>  
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  
@.planning/STATE.md
@.planning/phases/04-search-polish/04-CONTEXT.md
@.planning/phases/04-search-polish/04-RESEARCH.md

# Existing patterns and accessibility examples
@lib/presentation/providers/task_providers.dart
@lib/presentation/theme/app_theme.dart
@lib/data/datasources/local/task_local_datasource.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create date range filtering use case</name>
  <files>lib/domain/usecases/filter_tasks_by_date_range.dart</files>
  <action>
    Create FilterTasksByDateRange use case following clean architecture pattern from existing use cases:
    
    - Accept FilterTasksByDateRangeParams with startDate and endDate properties
    - Return Either<Failure, List<Task>>
    - Call repository method (will need to be added to interface)
    - Handle null date scenarios (no filtering applied)
    - Follow exact same structure as GetTasksByDate with proper error handling
    - Include documentation explaining date range is inclusive on both ends
    
    The use case should leverage existing TaskLocalDataSource.getTasksInDateRange() method that already exists in the data layer.
  </action>
  <verify>
    <automated>grep -q "class FilterTasksByDateRange" lib/domain/usecases/filter_tasks_by_date_range.dart && grep -q "Either<Failure, List<Task>>" lib/domain/usecases/filter_tasks_by_date_range.dart</automated>
  </verify>
  <done>FilterTasksByDateRange use case exists with proper Either return type and date handling</done>
</task>

<task type="auto">
  <name>Task 2: Create completion status filtering use case</name>
  <files>lib/domain/usecases/get_tasks_by_completion_status.dart</files>
  <action>
    Create GetTasksByCompletionStatus use case following established patterns:
    
    - Accept GetTasksByCompletionStatusParams with CompletionStatusFilter enum
    - Define enum: CompletionStatusFilter { all, completed, pending }
    - Return Either<Failure, List<Task>>
    - Handle "all" status by returning all tasks
    - For completed/pending, call appropriate repository methods
    - Follow same error handling pattern as other use cases
    
    Leverage existing TaskLocalDataSource.getTasksByCompletionStatus() method that already exists in the data layer.
  </action>
  <verify>
    <automated>grep -q "class GetTasksByCompletionStatus" lib/domain/usecases/get_tasks_by_completion_status.dart && grep -q "CompletionStatusFilter" lib/domain/usecases/get_tasks_by_completion_status.dart</automated>
  </verify>  
  <done>GetTasksByCompletionStatus use case exists with enum filter options and proper error handling</done>
</task>

<task type="auto">
  <name>Task 3: Build accessible search filters widget</name>
  <files>lib/presentation/widgets/search/search_filters.dart</files>
  <action>
    Create SearchFilters widget with comprehensive accessibility per D-10, D-11 requirements:
    
    Filter controls (per Claude's discretion for layout):
    - Date range filter with "From" and "To" date pickers 
    - Completion status filter with radio buttons (All, Completed, Pending)
    - Clear filters button to reset all filters
    - Collapsible design that starts expanded
    
    Accessibility implementation (critical for UI-04):
    - Wrap entire widget in Semantics with label "Search filters"
    - Each date picker button: semanticLabel with current selected date
    - Radio button group: Semantics with group label "Filter by completion status"
    - Clear button: semantic hint "Clears all active filters"
    - Use Focus management for proper tab order
    - Announce filter changes using SemanticsService.announce()
    
    Integration points:
    - Accept onFiltersChanged callback to update parent state
    - Show active filter count visually
    - Follow Material 3 design tokens from existing theme
    - Respect existing card styling and spacing patterns
  </action>
  <verify>
    <automated>grep -q "Semantics" lib/presentation/widgets/search/search_filters.dart && grep -q "showDatePicker" lib/presentation/widgets/search/search_filters.dart && grep -q "SemanticsService.announce" lib/presentation/widgets/search/search_filters.dart</automated>
  </verify>
  <done>SearchFilters widget exists with date range pickers, status radio buttons, and comprehensive accessibility features</done>
</task>

<task type="auto">  
  <name>Task 4: Integrate filters into search screen and state management</name>
  <files>lib/presentation/screens/search_screen.dart, lib/presentation/providers/search_providers.dart</files>
  <action>
    Extend search functionality to include filtering capabilities:
    
    In search_providers.dart:
    - Add FilterTasksByDateRangeUseCaseProvider and GetTasksByCompletionStatusUseCaseProvider
    - Extend SearchState to include dateRange and completionStatus filters
    - Update search logic to apply filters in combination with text search
    - Add methods: updateDateFilter(), updateStatusFilter(), clearFilters()
    - Ensure filtered results maintain D-07 ordering (most recent first)
    
    In search_screen.dart:
    - Add SearchFilters widget below SearchBar
    - Wire filter callbacks to provider methods
    - Update result display to show active filter count
    - Add filter state indicators in UI
    - Maintain existing auto-focus and debouncing behavior
    
    Combined search + filter logic:
    1. Text search first (if query >= 3 characters)
    2. Apply date range filter to results
    3. Apply completion status filter to results
    4. Order by creation date descending
  </action>
  <verify>
    <automated>grep -q "SearchFilters" lib/presentation/screens/search_screen.dart && grep -q "updateDateFilter" lib/presentation/providers/search_providers.dart && grep -q "clearFilters" lib/presentation/providers/search_providers.dart</automated>
  </verify>
  <done>Search screen integrated with filters and state management updated to handle combined search and filtering</done>
</task>

<task type="auto">
  <name>Task 5: Comprehensive accessibility audit and compliance verification</name>
  <files>accessibility_audit_results.md, lib/presentation/screens/task_list_screen.dart, lib/presentation/screens/main_screen.dart</files>
  <action>
    Conduct thorough accessibility audit to ensure WCAG 2.1 AA compliance across the entire app:
    
    Screen reader testing checklist:
    - Task creation/editing forms: proper labels, hints, error announcements
    - Calendar navigation: semantic labels for dates, month navigation
    - Task list: proper item labeling, action hints, list context
    - Search screen: search field labeling, results announcements, filter controls
    - Navigation: tab order, focus management, screen transitions
    
    WCAG compliance verification:
    - Color contrast ratios meet 4.5:1 minimum (already verified in theme)
    - Touch targets meet 44pt minimum (already verified in Phase 3)
    - Keyboard navigation works throughout app
    - Focus indicators visible and properly managed
    - Text scaling works up to 200% without content loss
    - Screen reader announcements are appropriate and informative
    
    Fixes to apply:
    - Add missing semantic labels to any unlabeled interactive elements
    - Improve focus management between screens
    - Add semantic hints for complex interactions
    - Ensure proper reading order for screen readers
    
    Document findings in accessibility_audit_results.md with:
    - Compliance verification for each WCAG criteria
    - Issues found and fixes applied
    - Testing methodology and tools used
    - Recommendations for ongoing accessibility maintenance
  </action>
  <verify>
    <automated>test -f accessibility_audit_results.md && grep -q "WCAG 2.1 AA" accessibility_audit_results.md && grep -q "Screen reader testing" accessibility_audit_results.md</automated>
  </verify>
  <done>Comprehensive accessibility audit completed, issues fixed, and compliance documented for WCAG 2.1 AA</done>
</task>

</tasks>

<verification>
- Date range filtering works with proper date picker controls
- Completion status filtering provides clear All/Completed/Pending options  
- Search and filters work together without conflicts
- All interactive elements have proper semantic labels and hints
- Screen reader navigation flows logically through all screens
- Focus management works correctly between screens and widgets
- Filter state is clearly indicated visually and announced to screen readers
- WCAG 2.1 AA compliance verified through systematic testing
</verification>

<success_criteria>
- User can filter search results by date range using native date pickers
- User can filter by completion status with visual indication of active filters
- Screen reader users can navigate entire app including search and filters
- All filtering controls meet accessibility guidelines with proper announcements
- Combined search and filtering provides coherent, useful results
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-polish/04-02-SUMMARY.md`
</output>