---
phase: 05-add-pomodoro-timer-feature
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - lib/presentation/screens/pomodoro_settings_screen.dart
  - lib/presentation/widgets/pomodoro/preset_selector.dart
  - lib/presentation/widgets/pomodoro/duration_sliders.dart
  - lib/presentation/navigation/app_router.dart
autonomous: true
requirements:
  - D-53
  - D-54
  - D-55
  - D-56

must_haves:
  truths:
    - "User can access settings screen from timer panel or navigation"
    - "User can select presets (Classic/Deep Work/Sprint/Custom)"
    - "Selecting Custom reveals duration sliders"
    - "All settings persist across app restarts"
  artifacts:
    - path: "lib/presentation/screens/pomodoro_settings_screen.dart"
      provides: "Complete settings screen"
      contains: "class PomodoroSettingsScreen"
    - path: "lib/presentation/widgets/pomodoro/preset_selector.dart"
      provides: "Segmented button for presets"
      contains: "class PresetSelector"
    - path: "lib/presentation/widgets/pomodoro/duration_sliders.dart"
      provides: "Work/break duration sliders"
      contains: "class DurationSliders"
  key_links:
    - from: "lib/presentation/screens/pomodoro_settings_screen.dart"
      to: "lib/presentation/providers/pomodoro_settings_providers.dart"
      via: "ref.watch(pomodoroSettingsProvider)"
      pattern: "pomodoroSettingsProvider"
    - from: "lib/presentation/navigation/app_router.dart"
      to: "lib/presentation/screens/pomodoro_settings_screen.dart"
      via: "GoRoute path"
      pattern: "pomodoro-settings"
---

<objective>
Build the Pomodoro settings screen with preset selector, duration sliders, behavior toggles, and daily goal configuration.

Purpose: Users need to customize timer durations, auto-start behavior, daily goals, volume, and penguin name. This screen controls all configurable aspects of the Pomodoro feature.

Output: Settings screen with all controls, preset selector widget, duration sliders widget, and routing integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-add-pomodoro-timer-feature/05-CONTEXT.md
@.planning/phases/05-add-pomodoro-timer-feature/05-UI-SPEC.md
@.planning/phases/05-add-pomodoro-timer-feature/05-01-SUMMARY.md
@.planning/phases/05-add-pomodoro-timer-feature/05-02-SUMMARY.md

<interfaces>
From Plan 02 - Settings providers:
```dart
final pomodoroSettingsProvider = AsyncNotifierProvider<PomodoroSettingsNotifier, PomodoroSettings>(...);
// Methods: updateSettings, applyPreset, updateVolume, updateTheme, updatePenguinName
```

From lib/presentation/navigation/app_router.dart:
```dart
class AppRouter {
  static final GoRouter router = GoRouter(routes: [...]);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preset selector and duration sliders widgets</name>
  <files>
    lib/presentation/widgets/pomodoro/preset_selector.dart,
    lib/presentation/widgets/pomodoro/duration_sliders.dart
  </files>
  <read_first>
    lib/presentation/theme/app_theme.dart,
    lib/presentation/providers/pomodoro_settings_providers.dart
  </read_first>
  <action>
**lib/presentation/widgets/pomodoro/preset_selector.dart** (per D-05, D-54):
- `class PresetSelector extends ConsumerWidget`
- Uses Material 3 `SegmentedButton<String>` with 4 segments:
  - "Classic (25/5/15)" value 'classic'
  - "Deep Work (50/10/20)" value 'deep_work'
  - "Sprint (15/3/10)" value 'sprint'
  - "Custom" value 'custom'
- Selected segment from `pomodoroSettingsProvider` value `.preset` field
- On selection changed: call `ref.read(pomodoroSettingsProvider.notifier).applyPreset(value)`
- When 'custom' selected: notify parent via callback `onCustomSelected(bool isCustom)` so parent can reveal DurationSliders

**lib/presentation/widgets/pomodoro/duration_sliders.dart** (per D-55):
- `class DurationSliders extends ConsumerWidget`
- 3 Material 3 `Slider` widgets with value labels:
  1. "Work Duration" — range 5-90 min, step 5 min, default from settings.workDurationMinutes
  2. "Short Break" — range 1-30 min, step 1 min, default from settings.shortBreakMinutes
  3. "Long Break" — range 5-60 min, step 1 min, default from settings.longBreakMinutes
- Each slider shows value label: "{N} min" in Label typography
- On change: update settings via `ref.read(pomodoroSettingsProvider.notifier).updateSettings(settings.copyWith(workDurationMinutes: value.round()))`
- Only visible when preset is 'custom' — parent wraps in `AnimatedSize` with 300ms easeOut curve
  </action>
  <verify>
    <automated>cd /home/ainews && dart analyze lib/presentation/widgets/pomodoro/preset_selector.dart lib/presentation/widgets/pomodoro/duration_sliders.dart 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "class PresetSelector"
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "SegmentedButton"
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "'classic'"
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "'deep_work'"
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "'sprint'"
    - lib/presentation/widgets/pomodoro/preset_selector.dart contains "'custom'"
    - lib/presentation/widgets/pomodoro/duration_sliders.dart contains "class DurationSliders"
    - lib/presentation/widgets/pomodoro/duration_sliders.dart contains "Slider"
    - lib/presentation/widgets/pomodoro/duration_sliders.dart contains "Work Duration" or "workDurationMinutes"
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>Preset selector with 4 options and duration sliders with proper ranges compile and use correct providers</done>
</task>

<task type="auto">
  <name>Task 2: Settings screen and router integration</name>
  <files>
    lib/presentation/screens/pomodoro_settings_screen.dart,
    lib/presentation/navigation/app_router.dart
  </files>
  <read_first>
    lib/presentation/navigation/app_router.dart,
    lib/presentation/screens/analytics_screen.dart,
    lib/presentation/widgets/pomodoro/preset_selector.dart,
    lib/presentation/widgets/pomodoro/duration_sliders.dart
  </read_first>
  <action>
**lib/presentation/screens/pomodoro_settings_screen.dart** (per D-53, UI-SPEC settings layout):
- `class PomodoroSettingsScreen extends ConsumerWidget`
- AppBar title: "Pomodoro Settings" with back button
- Single scrollable `ListView` with sections separated by 24px SizedBox:

**Section 1: Timer Presets**
- `PresetSelector` widget
- Below: `AnimatedSize` wrapping `DurationSliders` (visible only when preset == 'custom', 300ms easeOut)

**Section 2: Session Behavior**
- `SwitchListTile` "Auto-start breaks" — `settings.autoStartBreaks`, on toggle: update settings (per D-06)
- `SwitchListTile` "Auto-start work sessions" — `settings.autoStartWork`, on toggle: update settings (per D-06)
- `Slider` "Long break interval" — range 2-8, step 1, shows "{N} sessions" label, default 4 (per D-07)

**Section 3: Daily Goal**
- `Slider` "Target sessions per day" — range 1-20, step 1, default 8, shows "{N} sessions" (per D-24)

**Section 4: Sound**
- `Slider` "Pomodoro volume" — range 0.0-1.0, shows percentage "{N}%", calls `settingsNotifier.updateVolume(value)` (per D-41)

**Section 5: Theme** (placeholder — ThemePickerGrid comes from Plan 05)
- Text placeholder: "Theme selection" — will be replaced by ThemePickerGrid in Plan 05

**Section 6: Penguin**
- `TextField` "Penguin Name" with initial value `settings.penguinName` (default "Tux"), on submit: `settingsNotifier.updatePenguinName(value)` (per D-51)
- Placeholder 80x80 Container for penguin preview (filled in Plan 06)

Each section has a heading text in Heading typography (20px, semibold).

**lib/presentation/navigation/app_router.dart** — Add route:
```dart
GoRoute(
  path: '/pomodoro-settings',
  builder: (context, state) => const PomodoroSettingsScreen(),
),
```
Add import for PomodoroSettingsScreen.
  </action>
  <verify>
    <automated>cd /home/ainews && dart analyze lib/presentation/screens/pomodoro_settings_screen.dart lib/presentation/navigation/app_router.dart 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "class PomodoroSettingsScreen"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "Pomodoro Settings"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "PresetSelector"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "DurationSliders"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "SwitchListTile"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "Auto-start"
    - lib/presentation/screens/pomodoro_settings_screen.dart contains "Penguin Name" or "penguinName"
    - lib/presentation/navigation/app_router.dart contains "pomodoro-settings"
    - lib/presentation/navigation/app_router.dart contains "PomodoroSettingsScreen"
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>Settings screen shows all configurable options in scrollable sections. Route registered in GoRouter. All changes persist via settings provider to SharedPreferences.</done>
</task>

</tasks>

<verification>
- `dart analyze` passes for all modified files
- Settings screen accessible via route '/pomodoro-settings'
- All D-05, D-06, D-07, D-24, D-41, D-51, D-53, D-54, D-55 decisions implemented
</verification>

<success_criteria>
- Segmented button for presets with 4 options (D-05, D-54)
- Duration sliders appear on Custom selection with proper ranges (D-55)
- Auto-start toggles for breaks and work sessions (D-06)
- Long break interval slider (D-07)
- Daily goal slider (D-24)
- Volume slider with percentage (D-41)
- Penguin name text field (D-51)
- All settings persist via SharedPreferences
</success_criteria>

<output>
After completion, create `.planning/phases/05-add-pomodoro-timer-feature/05-04-SUMMARY.md`
</output>
