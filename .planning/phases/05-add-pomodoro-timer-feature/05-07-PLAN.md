---
phase: 05-add-pomodoro-timer-feature
plan: 07
type: execute
wave: 4
depends_on: ["05-03", "05-05", "05-06"]
files_modified:
  - lib/presentation/widgets/charts/pomodoro_charts.dart
  - lib/presentation/providers/pomodoro_analytics_providers.dart
  - lib/presentation/widgets/charts/productivity_charts.dart
  - lib/presentation/widgets/calendar/pomodoro_marker.dart
  - lib/presentation/widgets/calendar/day_calendar.dart
  - lib/presentation/widgets/calendar/week_calendar.dart
  - lib/presentation/widgets/pomodoro/pomodoro_badge.dart
  - lib/presentation/widgets/tasks/task_card.dart
autonomous: true
requirements:
  - D-20
  - D-21
  - D-22
  - D-26
  - D-27
  - D-28

must_haves:
  truths:
    - "Analytics tab shows daily focus sessions chart and per-task breakdown chart"
    - "Calendar day/week views show Pomodoro markers on task blocks"
    - "Task cards display Pomodoro session count badge"
    - "Analytics support weekly and monthly range views"
  artifacts:
    - path: "lib/presentation/widgets/charts/pomodoro_charts.dart"
      provides: "Pomodoro analytics charts"
      contains: "class PomodoroChartsWidget"
    - path: "lib/presentation/widgets/calendar/pomodoro_marker.dart"
      provides: "Calendar Pomodoro dot markers"
      contains: "class PomodoroCalendarMarker"
    - path: "lib/presentation/widgets/pomodoro/pomodoro_badge.dart"
      provides: "Task card session count badge"
      contains: "class PomodoroBadge"
  key_links:
    - from: "lib/presentation/widgets/charts/productivity_charts.dart"
      to: "lib/presentation/widgets/charts/pomodoro_charts.dart"
      via: "widget inclusion below existing charts"
      pattern: "PomodoroChartsWidget"
    - from: "lib/presentation/widgets/calendar/day_calendar.dart"
      to: "lib/presentation/widgets/calendar/pomodoro_marker.dart"
      via: "overlay on task blocks"
      pattern: "PomodoroCalendarMarker"
    - from: "lib/presentation/widgets/tasks/task_card.dart"
      to: "lib/presentation/widgets/pomodoro/pomodoro_badge.dart"
      via: "badge in card row"
      pattern: "PomodoroBadge"
---

<objective>
Integrate Pomodoro data into existing app surfaces: analytics charts, calendar markers, task card badges, and "Start Pomodoro" button.

Purpose: This plan connects the Pomodoro system to the rest of the app. Users see their focus data in analytics, completed sessions on calendar views, and session counts on task cards. This is where the Pomodoro feature becomes integrated rather than siloed.

Output: Pomodoro charts in analytics, calendar markers, task card badge and start button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-add-pomodoro-timer-feature/05-CONTEXT.md
@.planning/phases/05-add-pomodoro-timer-feature/05-UI-SPEC.md
@.planning/phases/05-add-pomodoro-timer-feature/05-01-SUMMARY.md
@.planning/phases/05-add-pomodoro-timer-feature/05-03-SUMMARY.md

<interfaces>
From Plan 01/02:
```dart
final pomodoroRepositoryProvider = Provider<PomodoroRepository>(...);
final taskPomodoroCountProvider = FutureProvider.family<int, int>(...); // count by taskId
final dailyPomodoroCountProvider = FutureProvider<int>(...);
```

From existing analytics:
```dart
final analyticsRangeProvider = StateProvider<AnalyticsRange>(...);
final productivityDataProvider = FutureProvider<ProductivityData>(...);
```

From existing calendar:
```dart
// DayCalendarWidget — renders hourly timeline with task blocks
// WeekCalendarWidget — renders weekly time grid with task blocks
```

From existing task card:
```dart
class TaskCard extends ConsumerWidget {
  final Task task;
  // Shows task info + edit/delete/completion actions
}
```

From Plan 02 timer:
```dart
final pomodoroTimerProvider = NotifierProvider<PomodoroTimerNotifier, PomodoroTimerState>(...);
// Methods: selectTask(taskId), startWork()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pomodoro analytics charts and providers</name>
  <files>
    lib/presentation/providers/pomodoro_analytics_providers.dart,
    lib/presentation/widgets/charts/pomodoro_charts.dart,
    lib/presentation/widgets/charts/productivity_charts.dart
  </files>
  <read_first>
    lib/presentation/widgets/charts/productivity_charts.dart,
    lib/presentation/widgets/charts/task_count_chart.dart,
    lib/presentation/widgets/charts/busy_minutes_chart.dart,
    lib/presentation/providers/analytics_providers.dart
  </read_first>
  <action>
**lib/presentation/providers/pomodoro_analytics_providers.dart:**
- `pomodoroAnalyticsDataProvider` — FutureProvider that:
  - Reads `analyticsRangeProvider` for weekly/monthly range
  - Queries pomodoro sessions in that date range via repository
  - Returns `PomodoroAnalyticsData` with:
    - `dailySessions`: `Map<DateTime, int>` — completed work sessions per day
    - `taskSessions`: `Map<int, int>` — completed work sessions per taskId
    - `taskNames`: `Map<int, String>` — task names for display (query from task repository)
    - `totalSessions`: int
    - `totalFocusMinutes`: int
    - `averageDailySessionsCount`: double

**lib/presentation/widgets/charts/pomodoro_charts.dart** (per D-20, D-21, D-22, UI-SPEC):
- `class PomodoroChartsWidget extends ConsumerWidget`
- Section title: "Focus Sessions" in Heading typography (20px)

**Chart 1: "Daily Focus Sessions"** (per D-21):
- fl_chart `BarChart` following existing `TaskCountChart` patterns
- X-axis: days of the range
- Y-axis: session count
- Bar color: `#E53935` (work red)
- Show value labels on top of bars
- Empty state: "No focus session data yet. Complete your first Pomodoro to see analytics."

**Chart 2: "Focus by Task"** (per D-21):
- fl_chart `BarChart` horizontal
- Y-axis: task names (truncated at 20 chars)
- X-axis: session count
- Bar color: `#1976D2` (primary blue)
- Show top 8 tasks by session count

**Summary stats row** (per D-22):
- "Total: {N} sessions"
- "Avg: {N}/day"
- "{H}h {M}m focused"
- Displayed in Label typography

Uses `analyticsRangeProvider` for weekly/monthly toggle — reuses the existing range selector from ProductivityChartsWidget.

**lib/presentation/widgets/charts/productivity_charts.dart** — Update:
- Import `PomodoroChartsWidget`
- Add `const SizedBox(height: 32)` after existing charts
- Add `const PomodoroChartsWidget()` below the spacer
- This integrates Pomodoro analytics into the existing Analytics tab per D-20
  </action>
  <verify>
    <automated>cd /home/ainews && dart analyze lib/presentation/providers/pomodoro_analytics_providers.dart lib/presentation/widgets/charts/pomodoro_charts.dart lib/presentation/widgets/charts/productivity_charts.dart 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/presentation/providers/pomodoro_analytics_providers.dart contains "pomodoroAnalyticsDataProvider"
    - lib/presentation/providers/pomodoro_analytics_providers.dart contains "dailySessions"
    - lib/presentation/providers/pomodoro_analytics_providers.dart contains "taskSessions"
    - lib/presentation/widgets/charts/pomodoro_charts.dart contains "class PomodoroChartsWidget"
    - lib/presentation/widgets/charts/pomodoro_charts.dart contains "Daily Focus Sessions"
    - lib/presentation/widgets/charts/pomodoro_charts.dart contains "Focus by Task"
    - lib/presentation/widgets/charts/pomodoro_charts.dart contains "BarChart"
    - lib/presentation/widgets/charts/productivity_charts.dart contains "PomodoroChartsWidget"
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>Two Pomodoro charts (daily totals + per-task breakdown) render in the existing Analytics tab below productivity charts. Weekly/monthly range selector controls both chart sets.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar markers, task card badge, and Start Pomodoro button</name>
  <files>
    lib/presentation/widgets/calendar/pomodoro_marker.dart,
    lib/presentation/widgets/calendar/day_calendar.dart,
    lib/presentation/widgets/calendar/week_calendar.dart,
    lib/presentation/widgets/pomodoro/pomodoro_badge.dart,
    lib/presentation/widgets/tasks/task_card.dart
  </files>
  <read_first>
    lib/presentation/widgets/calendar/day_calendar.dart,
    lib/presentation/widgets/calendar/week_calendar.dart,
    lib/presentation/widgets/tasks/task_card.dart
  </read_first>
  <action>
**lib/presentation/widgets/calendar/pomodoro_marker.dart** (per D-26, D-27):
- `class PomodoroCalendarMarker extends ConsumerWidget`
- Props: `int taskId`
- Reads `taskPomodoroCountProvider(taskId)` for count
- Renders small colored dots below parent widget:
  - Dot size: 6px diameter circles
  - Color: `#E53935` at 60% opacity
  - Max 4 visible dots in a Row with 2px spacing
  - If count > 4: show 3 dots + "+{N-3}" in 10px text
  - If count == 0: render nothing (SizedBox.shrink)
- Position: caller places this 2px below the task duration block

**lib/presentation/widgets/calendar/day_calendar.dart** — Update:
- Import PomodoroCalendarMarker
- Where task blocks are rendered (find the task block builder/positioned widget), add `PomodoroCalendarMarker(taskId: task.id)` positioned 2px below each task block
- This shows completed Pomodoro dots on day view task blocks

**lib/presentation/widgets/calendar/week_calendar.dart** — Update:
- Same integration as day_calendar: add PomodoroCalendarMarker below task blocks in week view

**lib/presentation/widgets/pomodoro/pomodoro_badge.dart** (per D-28, UI-SPEC):
- `class PomodoroBadge extends ConsumerWidget`
- Props: `int taskId`
- Reads `taskPomodoroCountProvider(taskId)`
- Only renders when count > 0
- Layout: small circular badge, 24px diameter
- Background: `#E53935` at 15% opacity
- Content: Row of `Icons.timer` (10px) + count text in Caption (12px, bold)
- Positioned right side of task card, vertically centered

**lib/presentation/widgets/tasks/task_card.dart** — Update:
- Import PomodoroBadge and PomodoroTimerNotifier
- Add `PomodoroBadge(taskId: task.id)` to the right side of the card content row
- Add expandable state to TaskCard:
  - Tap card to expand (currently cards don't have expand/collapse per Research open question 3)
  - In expanded view, add action row with existing Edit/Delete buttons
  - Add "Start Pomodoro" `OutlinedButton.icon` with `Icons.timer` (18px) and label "Start Pomodoro"
  - Only visible in expanded view (per D-17)
  - On tap: `ref.read(pomodoroTimerProvider.notifier).selectTask(task.id)` then `startWork()` if auto-start, otherwise just select and open panel
  - The button calls a callback or directly navigates — conceptually it sets the linked task and opens the timer panel
  </action>
  <verify>
    <automated>cd /home/ainews && dart analyze lib/presentation/widgets/calendar/pomodoro_marker.dart lib/presentation/widgets/pomodoro/pomodoro_badge.dart lib/presentation/widgets/tasks/task_card.dart lib/presentation/widgets/calendar/day_calendar.dart lib/presentation/widgets/calendar/week_calendar.dart 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/presentation/widgets/calendar/pomodoro_marker.dart contains "class PomodoroCalendarMarker"
    - lib/presentation/widgets/calendar/pomodoro_marker.dart contains "taskPomodoroCountProvider"
    - lib/presentation/widgets/calendar/pomodoro_marker.dart contains "0xFFE53935" or "Color(0xFFE53935)" or "#E53935"
    - lib/presentation/widgets/calendar/day_calendar.dart contains "PomodoroCalendarMarker"
    - lib/presentation/widgets/calendar/week_calendar.dart contains "PomodoroCalendarMarker"
    - lib/presentation/widgets/pomodoro/pomodoro_badge.dart contains "class PomodoroBadge"
    - lib/presentation/widgets/pomodoro/pomodoro_badge.dart contains "taskPomodoroCountProvider"
    - lib/presentation/widgets/tasks/task_card.dart contains "PomodoroBadge"
    - lib/presentation/widgets/tasks/task_card.dart contains "Start Pomodoro"
    - lib/presentation/widgets/tasks/task_card.dart contains "pomodoroTimerProvider" or "selectTask"
    - dart analyze reports no errors
  </acceptance_criteria>
  <done>Calendar views show Pomodoro markers. Task cards show session count badges. Expanded task cards have "Start Pomodoro" button. All integrate with existing Riverpod providers.</done>
</task>

</tasks>

<verification>
- `dart analyze` passes for all files
- Analytics tab includes Focus Sessions charts
- Day and week calendars show Pomodoro dot markers
- Task cards show badges and expanded Start Pomodoro button
</verification>

<success_criteria>
- Pomodoro data in existing Analytics tab (D-20)
- Daily totals AND per-task breakdown charts (D-21)
- Weekly + monthly summary views (D-22)
- Calendar day/week views show Pomodoro markers (D-26)
- Calendar markers are small colored dots (D-27)
- Task cards show Pomodoro count badge (D-28)
- "Start Pomodoro" button on expanded task card (D-09, D-17)
</success_criteria>

<output>
After completion, create `.planning/phases/05-add-pomodoro-timer-feature/05-07-SUMMARY.md`
</output>
